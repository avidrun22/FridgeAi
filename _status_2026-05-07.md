# Status — 2026-05-07 (Claude session, away-from-desk run)

Quick reference for when you're back. Newest at the top.

## Pending one-shot actions on your Mac

1. **Push the queued commit `e62b4b7`** (super-property fix for web analytics).
   The commit is local; sandbox couldn't push (no SSH tunnel through the
   egress proxy). Just:
   ```
   cd ~/fridgeai-native && git push
   ```
   Netlify will rebuild on push and the platform tag will start appearing
   on auto-fired events ($pageview, $identify) within a couple minutes.

2. **TestFlight build 20.** Submitted earlier. Apple's processing typically
   <1 hr. I tried to verify status via the App Store Connect API + browser
   but both paths require auth I won't take on your behalf:
   - The API needs a JWT request to Apple, which the sandbox proxy blocks.
   - The browser fallback hit Apple's 2FA login.
   Easiest check: the daily review-status Telegram cron will ping you
   when state changes, or the App Store Connect app on your phone shows
   "Ready to Test" once processing finishes.

3. **App Store / TestFlight metadata** — drafts are in chat scrollback
   (TestFlight "What to Test" + App Store "What's New in This Version").
   Both fields are editable in App Store Connect once build 20 finishes
   processing.

## Pushed and live (from earlier today)

- `aa101c7` — v1.15 instrumentation pass (5 new events + tour_started + UTM
  on digest CTAs)
- `8e22c0b` — v1.15 retention sprint (empty-state CTA, sample receipt, D1
  push, permission audit)
- `8028626` — v1.15 hotfix (sample-receipt rows + AddModal Cancel)
- `9120569` — Web app PostHog instrumentation (the one that closed the
  iOS-only data gap)
- `e62b4b7` — analytics super-property fix (LOCAL ONLY — see #1 above)

Web PostHog confirmed firing end-to-end: 5/7 DAU split shows 4 iOS / 1 web,
the 1 web event is from Claude's verification visit to app.ok2eat.com.

## Saved PostHog insights

- "DAU split — iOS vs web (28d)" (insight ID `qKw1Vafo`).
  More queries didn't get saved as insights — was mid-flight when you
  pinged about the push. Full canonical query catalog is in
  `docs/posthog-canonical-queries.md` — paste-and-run for now, save as
  insights from the SQL editor when you have time. Top 3 worth saving
  next: receipt-scan funnel (#2), D1/D7/D30 retention cohorts (#7),
  tour completion rate (#9).

## What still needs your hands

- Push e62b4b7 (above)
- Smoke-test v1.15 on real device when it lands in TestFlight — the
  empty-state flow, sample receipt, AddModal Cancel, D1 push at 6:30 PM
- iOS screenshots for Product Hunt launch (5 shots, ~30 min hands-on)
- Twitter thread + Reddit account warming (you've kept those)

## Misc files left in repo (gitignored or scratchpad)

- `_v1_15_commit_msg.txt` — already used, can delete
- `_v1_15_hotfix_msg.txt` — already used, can delete
- `_web_posthog_commit_msg.txt` — already used, can delete
- `_status_2026-05-07.md` — this file; delete when read
- `_adhd_launch_msg.txt`, `_twitter_thread_draft.txt` — reference

— Claude
